# HID Defender — Requirements

# Keyboard monitoring (cross-platform)
pynput>=1.7.6

# Web dashboard
flask>=2.3.0

# Windows-specific (only required on Windows)
# wmi - install via: pip install wmi pypiwin32 (Windows only)

# Optional: Linux udev Python bindings
# pyudev>=0.23.2
